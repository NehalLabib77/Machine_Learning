# NumPy — fast numerical operations

Purpose: short examples that demonstrate array creation, broadcasting, vectorized operations, and basic linear algebra.

- Open this first
- `Numpy/.Ipynb` — quick reference and examples.

What to open (other)
- N/A

How to use
- Run examples to see the performance differences vs Python lists; try array arithmetic and broadcasting.

Suggested exercises
- Convert a column from a pandas DataFrame into a NumPy array and perform a vectorized normalization.
- Use `np.dot` to compute a simple linear combination of feature arrays.

Notes
- NumPy underlies pandas and many ML libraries — mastering it speeds up data transformations.
