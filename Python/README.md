# Python — Fundamentals

Purpose: short, hands-on Python exercises to build the programming foundation needed for data work and machine learning.

Open this first
- `Python/.ipynb` — walkthrough of variables, control flow, functions, collections, and small exercises.

What to open (other)
- N/A

How to use
- Run the notebook kernel and execute cells top-to-bottom. Try editing small examples (e.g., write a function that computes accuracy).

Suggested exercises
- Implement a small utility that reads a CSV and returns the number of missing values per column.
- Write unit tests for a simple function using `assert` statements inside a notebook cell.

Notes
- This folder is the recommended starting point for absolute beginners.
